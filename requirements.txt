# Client application dependencies only.
# vLLM, PyTorch, and ROCm runtime packages run on Radeon Cloud.
# Do not add CUDA-specific packages such as torch+cu*, nvidia-*, cupy-cuda*, or CUDA-only vLLM wheels.
openai>=2.0.0,<3.0.0
httpx>=0.27.0,<1.0.0
