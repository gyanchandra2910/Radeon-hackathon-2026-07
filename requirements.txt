# Agent application dependencies only.
# vLLM, PyTorch, and ROCm runtime packages are installed/served separately by setup_vllm_rocm.sh.
# Do not add CUDA-specific packages such as torch+cu*, nvidia-*, cupy-cuda*, or CUDA-only vLLM wheels.
crewai>=1.15.0,<2.0.0
httpx>=0.27.0,<1.0.0
python-dotenv>=1.0.1,<2.0.0

